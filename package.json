{
  "scripts": {
    "bun": "bun build index.ts --minify --outdir dist",
    "rolldown": "rolldown index.ts -d dist --minify"
  },
  "dependencies": {
    "jimp": "^1.6.0",
    "rolldown": "^1.0.0-beta.38"
  }
}
